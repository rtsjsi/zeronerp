# Cloudflare API token (required for remote D1 + deploy)
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token

# JWT_SECRET is NOT stored here — it lives as a Cloudflare Worker secret.
# Set it once with: npm run secrets:jwt
